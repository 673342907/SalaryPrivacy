# 🚀 使用指定地址部署合约（成为 Admin）

## 目标
让 `0x6419cd60481d30528eb28005154169dd3c53e8b2` 成为 Admin。

## ✅ 操作步骤

### 步骤 1: 准备 MetaMask

1. 打开 MetaMask
2. 切换到账户：`0x6419cd60481d30528eb28005154169dd3c53e8b2`
3. 确保该地址有 Sepolia ETH（至少 0.01 ETH，用于支付 Gas）

### 步骤 2: 部署合约

在终端执行：

```powershell
# 进入 hardhat 目录
cd packages\hardhat

# 确保合约已编译
pnpm compile

# 部署合约（使用当前 MetaMask 账户）
pnpm exec hardhat run deploy/deploy-confidential-salary.ts --network sepolia
```

### 步骤 3: 复制合约地址

部署成功后会输出：
```
✅ 部署成功！
==========================================
合约名称: ConfidentialSalary
合约地址: 0xABC123...  ← 复制这个地址！
网络: Sepolia (Chain ID: 11155111)
==========================================
```

### 步骤 4: 更新前端配置

编辑文件：`packages/confidential-salary-frontend/src/config/contracts.ts`

```typescript
export const CONTRACT_ADDRESSES = {
  31337: '',
  11155111: '0x你的合约地址', // ← 粘贴步骤3复制的合约地址
};
```

### 步骤 5: 验证

1. 刷新前端页面（如果正在运行）
2. 使用 `0x6419cd60481d30528eb28005154169dd3c53e8b2` 连接钱包
3. 页面顶部应该显示：**"管理员 (Admin)"**
4. 应该能看到"角色权限管理"功能

## 🎯 完整命令

```powershell
# 1. 切换到 hardhat 目录
cd E:\code\fhe\ggg\fhevm-react-template-main\fhevm-react-template-main\packages\hardhat

# 2. 编译合约
pnpm compile

# 3. 在 MetaMask 切换到: 0x6419cd60481d30528eb28005154169dd3c53e8b2

# 4. 部署合约
pnpm exec hardhat run deploy/deploy-confidential-salary.ts --network sepolia

# 5. 复制输出的合约地址

# 6. 更新前端配置
# 编辑: packages/confidential-salary-frontend/src/config/contracts.ts

# 7. 重启前端（如果需要）
cd ..\confidential-salary-frontend
pnpm start
```

## ⚠️ 重要提示

1. **确保使用正确的账户**
   - MetaMask 中显示的账户应该是 `0x6419cd60481d30528eb28005154169dd3c53e8b2`
   - 部署时 Hardhat 会使用当前 MetaMask 账户

2. **合约地址 vs 钱包地址**
   - 钱包地址：`0x6419cd60481d30528eb28005154169dd3c53e8b2`（用于连接）
   - 合约地址：部署后得到的地址（需要填到前端配置）

3. **Gas 费用**
   - 确保账户有足够的 Sepolia ETH
   - 部署大约需要 0.01-0.02 ETH

## 🔍 如果部署失败

1. **检查网络**
   - 确保在 Sepolia 网络

2. **检查余额**
   - 确保有足够的 Sepolia ETH

3. **检查账户**
   - 确认 MetaMask 显示的是目标地址

---

**完成这些步骤后，`0x6419cd60481d30528eb28005154169dd3c53e8b2` 就会成为 Admin！** ✅

