# 🚀 正确启动本地开发服务器

## ❌ 错误方式

在项目根目录执行：
```powershell
pnpm dev  # ❌ 错误！根目录没有 dev 脚本
```

## ✅ 正确方式

### 方法 1: 进入 packages/nextjs 目录（推荐）

```powershell
# 1. 进入 Next.js 项目目录
cd packages\nextjs

# 2. 启动开发服务器
pnpm dev
```

### 方法 2: 从项目根目录使用 pnpm filter

```powershell
# 在项目根目录执行
pnpm --filter ./packages/nextjs dev
```

### 方法 3: 使用根目录的脚本（如果有）

检查根目录 `package.json` 中是否有相关脚本：
```powershell
# 查看可用脚本
pnpm run
```

## 🌐 访问应用

启动成功后，访问：
- **首页**: http://localhost:3000
- **ConfidentialSalary**: http://localhost:3000/confidential-salary

如果 3000 端口被占用，Next.js 会自动使用下一个可用端口（3001、3002 等）。

## 📝 完整启动流程

```powershell
# 1. 确保在项目根目录
cd E:\code\fhe\ggg\fhevm-react-template-main\SalaryPrivacy

# 2. 安装依赖（如果还没有）
pnpm install

# 3. 构建 SDK（如果需要）
pnpm sdk:build

# 4. 进入 Next.js 目录
cd packages\nextjs

# 5. 启动开发服务器
pnpm dev
```

## 🔍 验证服务器是否启动

终端会显示：
```
▲ Next.js 15.x.x
- Local:        http://localhost:3000
- Ready in X.Xs
```

## 🛑 停止服务器

在运行 `pnpm dev` 的终端中按 `Ctrl + C` 停止服务器。

---

**推荐使用方法 1：进入 packages/nextjs 目录后执行 pnpm dev** 🚀

