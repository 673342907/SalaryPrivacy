{
  "version": 2,
  "buildCommand": "echo '=== Step 1: Debug Info ===' && pwd && echo 'Root files:' && ls -la | head -20 && echo '=== Step 2: Check packages ===' && if [ -d 'packages' ]; then echo 'packages exists' && ls -la packages/; else echo 'ERROR: packages directory not found!'; exit 1; fi && echo '=== Step 3: Build SDK ===' && cd packages/fhevm-sdk && pnpm build && echo '=== Step 4: Build Frontend ===' && cd ../confidential-salary-frontend && pnpm build && echo '=== Step 5: Verify Output ===' && cd ../.. && if [ -d 'packages/confidential-salary-frontend/build' ]; then echo 'SUCCESS: Build directory found!' && ls -la packages/confidential-salary-frontend/build | head -10; else echo 'ERROR: Build directory not found!'; exit 1; fi",
  "outputDirectory": "packages/confidential-salary-frontend/build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/static/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}

