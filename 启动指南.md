# 🚀 项目启动和访问指南

## 📋 前置要求

在启动项目之前，请确保已安装：

1. **Node.js** >= 20.0.0
   ```powershell
   node --version
   ```

2. **pnpm** 包管理器
   ```powershell
   npm install -g pnpm
   pnpm --version
   ```

3. **MetaMask** 浏览器扩展（用于前端应用）

## 🎯 快速启动（3步）

### 步骤 1: 安装依赖

在项目根目录运行：

```powershell
# 进入项目目录（如果还没有）
cd E:\code\fhe\ggg\fhevm-react-template-main\SalaryPrivacy

# 安装所有依赖
pnpm install
```

**注意：** 首次安装可能需要几分钟时间。

### 步骤 2: 构建 SDK

```powershell
# 构建 FHEVM SDK
pnpm sdk:build
```

### 步骤 3: 启动应用

根据你想使用的框架，选择以下方式之一：

#### 选项 A: Next.js 应用（推荐）⭐

```powershell
# 从项目根目录运行
pnpm start
```

或者：

```powershell
pnpm --filter ./packages/nextjs dev
```

**访问地址：** `http://localhost:3001`

#### 选项 B: React 应用

```powershell
# 从项目根目录运行
pnpm --filter react-showcase start
```

**访问地址：** `http://localhost:3000`

#### 选项 C: Vue 应用

```powershell
# 从项目根目录运行
pnpm --filter vue-showcase dev
```

**访问地址：** `http://localhost:3003`

#### 选项 D: Node.js 命令行应用

```powershell
# 进入 node-showcase 目录
cd packages\node-showcase

# 交互式探索器（推荐）
pnpm explorer

# 或 HTTP 服务器模式
pnpm start
# 访问 http://localhost:3001
```

## 🔧 使用 PowerShell 启动脚本

项目提供了便捷的 PowerShell 脚本：

### React 应用

```powershell
.\start-react.ps1
```

### Node.js 应用

```powershell
.\start-node.ps1
```

## 📱 访问应用

启动成功后，在浏览器中打开对应的地址：

- **Next.js:** http://localhost:3001
- **React:** http://localhost:3000
- **Vue:** http://localhost:3003
- **Node.js HTTP:** http://localhost:3001

## 🔐 使用前准备

### 1. 连接 MetaMask 钱包

1. 确保已安装 MetaMask 浏览器扩展
2. 在应用中点击"连接钱包"按钮
3. 授权连接请求

### 2. 切换到 Sepolia 测试网

1. 在 MetaMask 中选择网络
2. 添加 Sepolia 测试网（如果还没有）：
   - 网络名称: Sepolia
   - RPC URL: https://sepolia.infura.io/v3/YOUR_KEY
   - 链 ID: 11155111
   - 货币符号: ETH

### 3. 获取测试币

如果钱包中没有 Sepolia ETH，可以从以下水龙头获取：

- https://sepoliafaucet.com/
- https://faucet.quicknode.com/ethereum/sepolia

## 🛠️ 常见问题

### 问题 1: packages 目录不存在

**解决方案：**

```powershell
# 检查是否在正确的目录
pwd

# 如果 packages 目录不存在，可能需要克隆完整的项目
# 或者检查项目是否已正确初始化
```

### 问题 2: 端口被占用

**解决方案：**

```powershell
# 查找占用端口的进程
netstat -ano | findstr :3000
netstat -ano | findstr :3001

# 结束进程（替换 PID 为实际进程 ID）
taskkill /PID <PID> /F
```

### 问题 3: 依赖安装失败

**解决方案：**

```powershell
# 清理并重新安装
pnpm sdk:clean
pnpm install
pnpm sdk:build
```

### 问题 4: SDK 构建失败

**解决方案：**

```powershell
# 确保 Node.js 版本 >= 20.0.0
node --version

# 清理并重新构建
pnpm sdk:clean
pnpm sdk:build
```

## 📝 开发模式

### 监听模式（自动重新编译）

```powershell
# 在一个终端运行 SDK 监听模式
pnpm sdk:watch

# 在另一个终端运行应用
pnpm start
```

### 运行测试

```powershell
# 运行所有测试
pnpm test

# 运行特定展示的测试
pnpm test:react
pnpm test:nextjs
pnpm test:vue
```

## 🎯 推荐工作流

### 第一次启动

1. ✅ 安装依赖：`pnpm install`
2. ✅ 构建 SDK：`pnpm sdk:build`
3. ✅ 启动应用：`pnpm start`（Next.js）
4. ✅ 在浏览器打开：http://localhost:3001
5. ✅ 连接 MetaMask 钱包
6. ✅ 切换到 Sepolia 测试网
7. ✅ 开始使用！

### 日常开发

1. 启动 SDK 监听：`pnpm sdk:watch`（在一个终端）
2. 启动应用：`pnpm start`（在另一个终端）
3. 在浏览器中访问应用
4. 开始开发！

## 📚 更多信息

- 详细文档：查看 `WINDOWS_GUIDE.md`
- 前端设置：查看 `FRONTEND_SETUP.md`
- 故障排除：查看 `TROUBLESHOOTING.md`

---

**祝你使用愉快！** 🎉


