# 🔧 解决设置后直接 404 问题

## 🚨 问题

在 Vercel Dashboard 中设置 Root Directory 和 Framework Preset 后，部署直接显示 404。

## 🔍 可能的原因

1. **构建没有真正执行**（还是 89 毫秒问题）
2. **Root Directory 设置后，Vercel 没有正确构建**
3. **输出目录配置错误**
4. **路由配置问题**

## ✅ 解决方案

### 方案 1: 检查构建日志（最重要！）

**在 Vercel Dashboard 中查看最新部署的构建日志：**

1. **Deployments → 最新部署**
2. **点击部署，查看构建日志**

**检查：**
- ✅ 是否执行了 `pnpm install`？
- ✅ 是否执行了 `next build`？
- ✅ 构建时间是多少？（应该是几秒钟，不是 89 毫秒）
- ✅ 是否显示了路由信息？

### 方案 2: 调整 Vercel Dashboard 设置

**如果构建日志显示构建成功但仍然是 404：**

1. **Settings → General → Build & Development Settings**
   - **Framework Preset**: Next.js
   - **Build Command**: 设置为 `pnpm run build`（明确指定）
   - **Install Command**: 设置为 `pnpm install`（明确指定）
   - **Output Directory**: **留空**（不要填任何值）
   - 点击 **Save**

2. **清除缓存并重新部署**
   - Deployments → 最新部署 → "..." → "Redeploy"
   - 取消勾选 "Use existing Build Cache"
   - 点击 "Redeploy"

### 方案 3: 检查 packages/nextjs/vercel.json

**确保 `packages/nextjs/vercel.json` 配置正确：**

```json
{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build"
}
```

**不要设置 `outputDirectory`，让 Next.js 自动处理。**

### 方案 4: 使用 Vercel CLI 部署（绕过 Dashboard 配置）

**如果 Dashboard 配置一直有问题，使用 CLI：**

```powershell
# 进入 packages/nextjs 目录
cd packages\nextjs

# 删除 .vercel 目录（如果有）
Remove-Item -Recurse -Force .vercel -ErrorAction SilentlyContinue

# 重新链接
vercel link
# 选择项目：salary-privacy
# Root Directory: .（当前目录）

# 部署
vercel --prod --force
```

## 🔍 诊断步骤

### 步骤 1: 查看构建日志

**在 Vercel Dashboard 中：**
- Deployments → 最新部署 → 查看构建日志

**告诉我：**
- 构建时间是多少？
- 是否执行了 `next build`？
- 是否显示了路由信息？
- 是否有任何错误？

### 步骤 2: 检查 Root Directory 设置

**在 Vercel Dashboard 中：**
- Settings → General → Root Directory
- 确认设置为：`packages/nextjs`（没有斜杠，没有空格）

### 步骤 3: 检查 Framework Preset

**在 Vercel Dashboard 中：**
- Settings → Build & Development Settings → Framework Preset
- 确认设置为：**Next.js**

## 📝 立即操作

**请先告诉我构建日志的内容：**
1. 构建时间是多少？
2. 是否执行了 `next build`？
3. 是否有任何错误信息？

**这样我可以更准确地诊断问题！**

---

**或者，如果方便，直接使用 CLI 部署：**
```powershell
cd packages\nextjs
vercel --prod --force
```

