# 🚀 触发 Vercel 部署 - 替代方案

## 🔍 问题

在 Vercel Dashboard 的 "Redeploy" 对话框中没有 "Commit" 下拉菜单。

## ✅ 解决方案

### 方案 1: 通过 GitHub 推送新提交触发自动部署（推荐）

**最简单的方法：创建一个小的更改并推送，Vercel 会自动检测并部署最新提交。**

#### 步骤 1: 创建一个小的更改

```powershell
# 在根目录创建一个空文件或更新现有文件
echo "# Vercel 部署触发" > .vercel-deploy-trigger
```

或者更新 `vercel.json` 添加一个注释（虽然 JSON 不支持注释，但我们可以添加一个不影响功能的字段）：

```json
{
  "version": 2,
  "buildCommand": "cd test-app && npm install && npm run build",
  "installCommand": "cd test-app && npm install",
  "framework": "nextjs",
  "outputDirectory": "test-app/.next",
  "_deployTrigger": "2025-12-02"
}
```

#### 步骤 2: 提交并推送

```powershell
git add vercel.json
git commit -m "触发 Vercel 部署 - 使用最新提交"
git push
```

#### 步骤 3: 等待 Vercel 自动部署

- Vercel 会自动检测到新提交
- 自动开始部署
- 使用最新的提交（`fb893cc` 或更新的）

### 方案 2: 在 Vercel Dashboard 中取消当前部署并重新部署

1. **进入 Deployments**
   - 点击左侧菜单的 **"Deployments"** 标签

2. **取消当前部署（如果有正在进行的）**
   - 找到正在构建的部署
   - 点击 **"..."** → **"Cancel"**

3. **创建新部署**
   - 点击右上角的 **"Deploy"** 按钮（如果有）
   - 或者等待 Vercel 自动检测新提交

### 方案 3: 检查 Vercel 项目设置

1. **Settings → Git**
   - 确认 GitHub 仓库连接正常
   - 确认分支是 `main`
   - 如果有问题，重新连接

2. **Settings → General**
   - 确认 **Production Branch** 是 `main`
   - 确认 **Root Directory** 是 **留空**（不是 `test-app`）

### 方案 4: 使用 Vercel CLI（如果安装了）

如果你安装了 Vercel CLI：

```powershell
# 安装 Vercel CLI（如果还没有）
npm install -g vercel

# 登录
vercel login

# 部署（会使用当前目录的代码）
vercel --prod
```

## 🎯 推荐操作流程

**最简单的方法：**

1. **更新 `vercel.json` 添加一个时间戳字段**
   ```json
   {
     "version": 2,
     "buildCommand": "cd test-app && npm install && npm run build",
     "installCommand": "cd test-app && npm install",
     "framework": "nextjs",
     "outputDirectory": "test-app/.next",
     "_deployTrigger": "2025-12-02-10-00"
   }
   ```

2. **提交并推送**
   ```powershell
   git add vercel.json
   git commit -m "触发 Vercel 部署"
   git push
   ```

3. **在 Vercel Dashboard 中观察**
   - 进入 **Deployments**
   - 应该会看到新的部署自动开始
   - 确认使用的是最新提交

## 🔍 验证部署

**部署开始后，检查构建日志：**

### 1. 确认提交正确

构建日志的第一行应该显示：
```
克隆 github.com/673342907/SalaryPrivacy（分支：main，提交：xxxxx）
```

提交应该是 `fb893cc` 或更新的。

### 2. 确认执行构建命令

构建日志应该显示：
```
Running "install" command: `cd test-app && npm install`...
```

### 3. 确认 Next.js 构建

构建日志应该显示：
```
Creating an optimized production build...
Compiled successfully
```

### 4. 确认路由信息

构建日志应该显示：
```
Route (app)                                 Size  First Load JS
┌ ○ /                                      127 B         102 kB
```

### 5. 确认构建时间

构建时间应该需要 **5-10 秒**，而不是 113 毫秒。

## ✅ 完成后的检查清单

- [ ] 推送了新提交到 GitHub
- [ ] Vercel 自动检测到新提交并开始部署
- [ ] 构建日志显示提交是 `fb893cc` 或更新的
- [ ] 构建日志显示 `cd test-app && npm install`
- [ ] 构建日志显示 Next.js 构建输出
- [ ] 构建日志显示路由信息
- [ ] 构建时间需要几秒钟（不是 113 毫秒）
- [ ] 访问 URL 显示 "🚀 Vercel 测试应用"
- [ ] 没有 404 错误

---

**推荐：使用方案 1（推送新提交），这是最简单可靠的方法！** 🚀

