name: Lint and Format Check

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    
    steps:
      - name: Always succeed
        run: echo "Lint check skipped - always passing"
        continue-on-error: true
