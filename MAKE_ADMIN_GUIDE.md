# 🔐 让指定地址成为 Admin 的完整指南

## 目标
让钱包地址 `0x6419cd60481d30528eb28005154169dd3c53e8b2` 成为 Admin。

## 方法 1: 使用该地址部署合约（推荐，最简单）

这是最简单的方法，部署者自动成为 Admin。

### 步骤：

1. **在 MetaMask 中切换到目标地址**
   - 打开 MetaMask
   - 切换到账户：`0x6419cd60481d30528eb28005154169dd3c53e8b2`
   - 确保该地址有 Sepolia ETH（用于支付 Gas）

2. **部署合约**
   ```powershell
   cd packages\hardhat
   pnpm exec hardhat run deploy/deploy-confidential-salary.ts --network sepolia
   ```

3. **复制合约地址**
   部署成功后会输出：
   ```
   ✅ 部署成功！
   合约地址: 0xABC123...  ← 复制这个地址
   ```

4. **更新前端配置**
   ```typescript
   // packages/confidential-salary-frontend/src/config/contracts.ts
   export const CONTRACT_ADDRESSES = {
     11155111: '0x你的合约地址', // ← 粘贴这里
   };
   ```

5. **连接钱包并验证**
   - 使用 `0x6419cd60481d30528eb28005154169dd3c53e8b2` 连接钱包
   - 应该显示"管理员 (Admin)"

## 方法 2: 如果合约已部署，转移 Admin 角色

如果合约已经部署，但使用的是其他地址，需要转移 Admin。

### 前提条件：
- 当前 Admin 账户可以访问
- 合约包含 `transferAdmin` 函数（已添加）

### 步骤：

1. **使用当前 Admin 账户连接钱包**
   - 连接部署合约的账户

2. **打开角色管理页面**
   - 在页面底部找到"角色权限管理"

3. **转移 Admin 角色**
   - 点击"转移 Admin 角色"按钮
   - 输入新地址：`0x6419cd60481d30528eb28005154169dd3c53e8b2`
   - 确认转移

4. **使用新地址连接**
   - 断开当前连接
   - 使用 `0x6419cd60481d30528eb28005154169dd3c53e8b2` 连接
   - 应该显示"管理员 (Admin)"

## 方法 3: 如果合约已部署但没有 transferAdmin 函数

如果合约是旧版本，没有 `transferAdmin` 函数，需要重新部署。

### 步骤：

1. **重新编译合约**（包含 transferAdmin）
   ```powershell
   cd packages\hardhat
   pnpm compile
   ```

2. **使用新地址部署**
   - 在 MetaMask 切换到 `0x6419cd60481d30528eb28005154169dd3c53e8b2`
   - 部署新合约
   - 更新前端配置

## 🎯 推荐操作流程

### 如果合约未部署或数据不重要：

```powershell
# 1. 切换到目标地址的 MetaMask
# 地址: 0x6419cd60481d30528eb28005154169dd3c53e8b2

# 2. 编译合约
cd packages\hardhat
pnpm compile

# 3. 部署合约（该地址自动成为 Admin）
pnpm exec hardhat run deploy/deploy-confidential-salary.ts --network sepolia

# 4. 复制输出的合约地址

# 5. 更新前端配置
# 编辑: packages/confidential-salary-frontend/src/config/contracts.ts
# 将合约地址粘贴到 11155111 对应的值

# 6. 重启前端（如果正在运行）
cd ..\confidential-salary-frontend
pnpm start

# 7. 使用目标地址连接钱包
# 应该显示"管理员 (Admin)"
```

## ⚠️ 重要提示

1. **合约地址 vs 钱包地址**
   - 钱包地址：`0x6419cd60481d30528eb28005154169dd3c53e8b2`（用于连接和部署）
   - 合约地址：部署后得到的地址（需要填到前端配置）

2. **数据保留**
   - 重新部署会丢失所有数据
   - 如果数据重要，使用转移功能

3. **验证 Admin**
   - 连接钱包后，页面顶部应该显示"管理员 (Admin)"
   - 应该能看到"角色权限管理"功能

## 🔍 验证步骤

部署或转移后，验证是否成功：

1. **使用目标地址连接钱包**
   - `0x6419cd60481d30528eb28005154169dd3c53e8b2`

2. **检查角色显示**
   - 页面顶部应该显示"管理员 (Admin)"

3. **测试 Admin 功能**
   - 应该能看到"角色权限管理"
   - 可以创建部门
   - 可以管理员工

---

**按照以上步骤操作，`0x6419cd60481d30528eb28005154169dd3c53e8b2` 就会成为 Admin！** ✅

