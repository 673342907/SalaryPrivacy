# 🏗️ 系统架构文档

## 📊 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    User Interface                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Browser    │  │  MetaMask    │  │   Wallet     │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘ │
└─────────┼──────────────────┼──────────────────┼─────────┘
          │                  │                  │
┌─────────▼──────────────────▼──────────────────▼─────────┐
│              React Frontend (Next.js)                   │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Components                                       │  │
│  │  - Dashboard                                      │  │
│  │  - DepartmentManagement                          │  │
│  │  - EmployeeManagement                            │  │
│  │  - SalaryManagement                              │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Hooks                                           │  │
│  │  - useConfidentialSalary                         │  │
│  │  - useFhevm                                      │  │
│  │  - useFHEEncryption                              │  │
│  └──────────────────────────────────────────────────┘  │
└─────────┬───────────────────────────────────────────────┘
          │
┌─────────▼───────────────────────────────────────────────┐
│              FHEVM SDK Layer                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Encryption/Decryption                           │  │
│  │  - encryptWith()                                 │  │
│  │  - decrypt()                                     │  │
│  │  - prepareDecryptRequest()                      │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Relayer Communication                          │  │
│  │  - Request decryption signatures                 │  │
│  │  - Handle encrypted transactions                 │  │
│  └──────────────────────────────────────────────────┘  │
└─────────┬───────────────────────────────────────────────┘
          │
┌─────────▼───────────────────────────────────────────────┐
│              Web3 Layer (Wagmi)                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Contract Interaction                           │  │
│  │  - useReadContract                              │  │
│  │  - useWriteContract                             │  │
│  │  - useWaitForTransactionReceipt                 │  │
│  └──────────────────────────────────────────────────┘  │
└─────────┬───────────────────────────────────────────────┘
          │
┌─────────▼───────────────────────────────────────────────┐
│         Smart Contracts (Solidity)                     │
│  ┌──────────────────────────────────────────────────┐  │
│  │  ConfidentialSalary.sol                          │  │
│  │  - 加密数据存储                                   │  │
│  │  - 权限管理                                      │  │
│  │  - FHE 计算操作                                  │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Example Contracts (6个)                        │  │
│  │  - FHEAccessControl                              │  │
│  │  - FHEInputProof                                 │  │
│  │  - FHEAntipatterns                              │  │
│  │  - FHEBlindAuction                               │  │
│  │  - FHEArithmetic                                 │  │
│  │  - FHEComparison                                 │  │
│  └──────────────────────────────────────────────────┘  │
└─────────┬───────────────────────────────────────────────┘
          │
┌─────────▼───────────────────────────────────────────────┐
│         Ethereum Sepolia (FHEVM Network)               │
│  ┌──────────────────────────────────────────────────┐  │
│  │  FHEVM Relayer                                  │  │
│  │  - 处理加密交易                                   │  │
│  │  - 生成解密签名                                   │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 🔄 数据流程

### 1. 加密数据提交流程

```
用户输入 (明文)
    ↓
FHEVM SDK 加密
    ↓
生成加密数据 + 输入证明
    ↓
调用智能合约 (submitSalary)
    ↓
合约验证输入证明
    ↓
存储加密数据 (euint32)
    ↓
区块链存储
```

### 2. 加密数据查询流程

```
用户请求查看薪资
    ↓
调用智能合约 (getEncryptedSalary)
    ↓
返回加密数据 (bytes)
    ↓
FHEVM SDK 准备解密请求
    ↓
Relayer 生成解密签名
    ↓
FHEVM SDK 解密
    ↓
用户看到明文
```

### 3. 加密计算流程

```
请求部门总薪资
    ↓
合约获取所有员工加密薪资
    ↓
使用 TFHE.add() 累加 (加密状态)
    ↓
返回加密的总和
    ↓
（可选）用户解密查看
```

## 🔐 安全架构

### 加密层

- **客户端加密**: 所有数据在客户端使用 FHEVM SDK 加密
- **输入证明**: 每个加密数据都包含输入证明，防止伪造
- **链上存储**: 加密数据存储在区块链上，永久保存

### 访问控制层

- **FHE.allowThis()**: 允许合约存储加密值
- **FHE.allow()**: 允许特定地址访问加密值
- **FHE.allowTransient()**: 临时访问（仅本次调用）

### 权限控制层

- **基于角色的访问控制 (RBAC)**
- **智能合约级别的权限检查**
- **前端 UI 级别的权限提示**

## 📦 模块划分

### 前端模块

```
packages/nextjs/
├── app/
│   └── confidential-salary/
│       ├── _components/      # UI 组件
│       ├── _context/         # 数据上下文
│       └── page.tsx          # 主页面
├── hooks/
│   └── confidential-salary/ # 业务逻辑 Hooks
└── contracts/
    └── deployedContracts.ts  # 合约配置
```

### 智能合约模块

```
packages/hardhat/
├── contracts/
│   ├── ConfidentialSalary.sol  # 主合约
│   └── examples/                # 示例合约（6个）
├── test/                        # 测试文件（3个）
└── scripts/
    ├── deploy.ts                # 部署脚本
    └── generate-docs.ts         # 文档生成
```

### 工具模块

```
packages/create-fhevm-example/
├── src/
│   ├── cli.ts                  # CLI 入口
│   ├── createExample.ts        # 创建示例
│   └── createCategory.ts       # 创建分类
└── templates/                  # 模板文件
```

## 🔧 技术栈详解

### 前端技术栈

- **Next.js 15.2.5**: React 框架，支持 SSR 和 SSG
- **React 19**: UI 库
- **Wagmi 2.16.4**: Web3 React Hooks
- **RainbowKit**: 钱包连接 UI
- **FHEVM SDK**: 全同态加密 SDK
- **Tailwind CSS**: 样式框架
- **Recharts**: 数据可视化

### 智能合约技术栈

- **Solidity 0.8.24**: 智能合约语言
- **Hardhat**: 开发环境
- **FHEVM Solidity**: FHE 合约库
- **Chai + Mocha**: 测试框架

### 开发工具

- **TypeScript**: 类型安全
- **ESLint**: 代码检查
- **Prettier**: 代码格式化
- **Solhint**: Solidity 代码检查
- **Solidity Coverage**: 测试覆盖率

## 🚀 部署架构

### 开发环境

```
本地开发
    ↓
Hardhat 本地网络
    ↓
FHEVM 本地 Relayer
```

### 测试环境

```
GitHub Actions
    ↓
自动测试
    ↓
Sepolia 测试网
```

### 生产环境

```
Vercel (前端)
    ↓
Sepolia 测试网 (合约)
    ↓
FHEVM Relayer
```

## 📊 性能考虑

### Gas 优化

- 使用事件记录而非存储
- 批量操作减少交易次数
- 优化数据结构

### 前端优化

- 代码分割
- 懒加载组件
- 缓存策略

## 🔒 安全考虑

### 智能合约安全

- 输入验证
- 权限检查
- 重入攻击防护
- 整数溢出防护（Solidity 0.8+）

### 前端安全

- 输入验证
- XSS 防护
- 钱包连接安全
- 加密数据保护

---

**最后更新：** 2024-12-03

