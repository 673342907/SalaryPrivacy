# 🚀 立即操作步骤 - 修复 Next.js 识别问题

## ✅ 已完成的修复

1. ✅ **重命名根目录的 `vercel.json`** → `vercel.json.backup`（避免冲突）
2. ✅ **更新 `test-app/vercel.json`**（添加完整的构建配置）
3. ✅ **已提交更改到本地 Git**

## 📋 下一步操作

### 步骤 1: 推送到 GitHub

```powershell
git push
```

### 步骤 2: 在 Vercel Dashboard 中设置（关键！）

1. **访问 Vercel Dashboard**
   - https://vercel.com
   - 进入你的项目

2. **Settings → General**
   - **Root Directory**: 输入 `test-app`（确保没有前导斜杠）
   - 点击 **Save**

3. **Settings → General → Build & Development Settings**
   - **Framework Preset**: 选择 **Next.js**（如果还没有选择）
   - **Build Command**: 留空（让 Vercel 自动检测，或使用 `npm run build`）
   - **Install Command**: 留空（让 Vercel 自动检测，或使用 `npm install`）
   - 点击 **Save**

### 步骤 3: 清除缓存并重新部署

1. **进入 Deployments**
2. **点击失败的部署** → **"..."** → **"Redeploy"**
3. **重要**：**取消勾选** "Use existing Build Cache"
4. **选择最新提交**（应该是最新的 commit）
5. **点击 "Redeploy"**

## 🔍 验证步骤

部署成功后，检查：

- ✅ 构建日志中没有 "无法识别 Next.js 版本" 的警告
- ✅ 构建成功完成
- ✅ 部署成功
- ✅ 可以访问应用（显示 "Hello Vercel!"）

## 💡 如果仍然失败

### 检查 1: Root Directory 设置

- 确保输入的是 `test-app`（不是 `/test-app` 或 `./test-app`）
- 确保没有多余的空格
- 保存后刷新页面，确认设置已保存

### 检查 2: GitHub 上的文件

访问 https://github.com/673342907/SalaryPrivacy/tree/main/test-app

确认以下文件存在：
- ✅ `package.json`（包含 `"next": "^15.2.3"`）
- ✅ `vercel.json`
- ✅ `next.config.js`
- ✅ `app/page.tsx`

### 检查 3: 构建日志

查看构建日志中的：
- **工作目录**：应该是 `/vercel/path0/test-app`
- **package.json 位置**：应该能找到 `test-app/package.json`
- **Next.js 检测**：应该能看到 Next.js 版本信息

## 🆘 最后的解决方案

如果上述所有步骤都不工作，考虑：

1. **创建新的 Vercel 项目**
   - Vercel Dashboard → Add New Project
   - 选择同一个 GitHub 仓库
   - Root Directory: `test-app`
   - 直接部署

2. **或者，将 test-app 提取到新仓库**
   - 在 GitHub 上创建新仓库
   - 将 `test-app` 目录的内容推送到新仓库
   - 在 Vercel 中导入新仓库（会自动检测 Next.js）

